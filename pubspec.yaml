name: custom_grid_count_view
description: "A flutter package that render dynamic height grid count view with swipe to action feature."
version: 1.0.1
homepage: https://github.com/subhashDev11/custom_grid_count_view

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_slidable: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


# The following section is specific to Flutter packages.
#  custom_grid_count_view: any
flutter:

